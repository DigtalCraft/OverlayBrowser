# Overlay Browser v1.0.5

公開日: 2026年8月8日

## 主な変更

### Windows起動とタスクトレイ常駐の安定化

- 「設定」から、Windowsへのサインイン時にOverlay Browserを自動起動できるようにしました。
- Windows自動起動時は画面を開かず、タスクトレイへ常駐します。
- Windowsログイン直後にChromiumの初期化で異常終了する場合があったため、ブラウザの初期化を画面表示時まで遅らせるようにしました。
- ショートカットまたはEXEから通常起動した場合は、画面を表示した状態でタスクトレイにも常駐します。
- 閉じるボタンまたは `Alt + F4` ではアプリを終了せず、画面をタスクトレイへ格納します。完全に終了する場合は、タスクトレイメニューの「終了」を選択してください。

### Webサイトの操作性改善

- PayPalなどの決済・本人確認サイトで、認証用ポップアップを操作しやすくしました。
- 埋め込みブラウザで利用できないページを、Windowsの標準ブラウザで開けるメニューを追加しました。
- ChatGPTなどの入力フォームで、クリック後に文字入力できない場合がある問題を修正しました。

### 内部構成と保守性の改善

- 画面処理の一部をViewModelとサービスへ分離し、既存機能を保ちながらコードを整理しました。
- CefSharpなどの依存ライブラリ更新を確認するDependabot設定と、更新時のReleaseビルド確認を追加しました。

## インストール方法

1. 起動中のOverlay Browserを、タスクトレイメニューの「終了」から終了します。
2. `Setup.msi` を実行します。
3. 旧バージョンがインストールされている場合は、設定を引き継いだままv1.0.5へ更新されます。

## 配布ファイル

- `Setup.msi`
